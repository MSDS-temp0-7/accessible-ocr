"""F1 · 본문 OCR — 레이아웃 검출과 독립적으로 페이지 전체 텍스트를 인식.

`daisy_ocr.layout.detect`가 "여기가 무슨 유형의 영역인가"를 담당한다면,
이 패키지는 "이 페이지에 어떤 글자가 있는가"를 담당한다. 둘을 분리한 이유:
레이아웃 모델이 어떤 텍스트 조각에 박스를 못 그리면(recall < 100%), 그 안의
글자는 레이아웃 박스를 거치는 파이프라인에서 통째로 사라진다. 전체 페이지를
독립적으로 OCR하면 박스 검출 여부와 무관하게 텍스트를 확보할 수 있다.
"""
